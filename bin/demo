#!/usr/bin/env bash
# One-command sandbox: clone the repo, run bin/demo, open the printed URL.
# Fresh sqlite database + rich sample data on every boot.
set -euo pipefail
cd "$(dirname "$0")/.."

bundle check >/dev/null 2>&1 || bundle install

exec bundle exec ruby bin/demo_boot.rb
